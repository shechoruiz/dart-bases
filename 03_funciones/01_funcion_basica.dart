void main () {
  saludar();
}

// Lo ideal es definir el tipo de dato que va a retornar la funcion. Si esta no se define, por defecto es void y debe declararse como tal.

String saludar() {
  // print('Hola $nombre');
  return 'Hola Sergio';
}